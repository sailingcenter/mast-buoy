# Building
```shell
pio run
```

# Flashing
```shell
pio run -t upload
```

# Serial Monitor
```shell
pio run -t monitor
```
can also be combined with flashing:
```shell
pio run -t upload -t monitor
```